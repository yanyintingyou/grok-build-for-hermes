# Agent Instructions — Grok Build for Agent

This repository packages the `grok-build-for-agent` AgentSkills-compatible skill.

For Codex/OpenAI-style agents:

1. Treat the root `SKILL.md` as the canonical skill instructions when working in this repository.
2. If installing as a reusable skill, copy `skills/autonomous-ai-agents/grok-build-for-agent/` to your agent skills directory as `grok-build-for-agent/`.
3. Do not use stale paths from older layouts; the packaged copy is now at `skills/autonomous-ai-agents/grok-build-for-agent/SKILL.md`.

Follow the privacy, safety, and verification rules in `SKILL.md`.
