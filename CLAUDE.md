# Claude Code Instructions — Grok Build for Agent

Use the root `SKILL.md` as the canonical instructions for this repository.

For Claude Code skill installation, copy the packaged skill directory:

```text
skills/autonomous-ai-agents/grok-build-for-agent/
```

to:

```text
~/.claude/skills/autonomous-ai-agents/grok-build-for-agent/
```

The directory name and the `name:` field are both `grok-build-for-agent`. Do not reference older repository layouts.
